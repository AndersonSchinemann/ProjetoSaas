<!-- Adicione Bootstrap CSS e AngularJS -->
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
<script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.8.2/angular.min.js"></script>

<style>
  /* Adicione um estilo personalizado para ajustar a largura dos inputs */
  .custom-width-input {
    width: 50%; /* Largura desejada */
  }
</style>

<!-- Seus scripts Angular -->
<script src="js/controllerFiltro.js"></script>

<div class="container text-left" ng-app="CadastroApp" ng-controller="CadastroController">

  <h2>Lista de Usuários</h2>
  <table class="table">
    <thead>
      <tr>
        <th>Nome</th>
        <th>Email</th>
        <th>Senha</th>
        <th>Perfil</th>
      </tr>
    </thead>
    <tbody>
      <tr ng-repeat="user in usuarios">
        <td>{{ user.nome }}</td>
        <td>{{ user.email }}</td>
        <td>{{ user.senha }}</td>
        <td>{{ user.perfil }}</td>
      </tr>
    </tbody>
  </table>

  <ul class="pagination">
    <li class="page-item" ng-class="{ 'disabled': currentPage === 1 }">
      <a class="page-link" href="#" ng-click="prevPage()">Anterior</a>
    </li>

    <li class="page-item" ng-class="{ 'disabled': currentPage === totalPages }">
      <a class="page-link" href="#" ng-click="nextPage()">Próxima</a>
    </li>
  </ul>
</div>